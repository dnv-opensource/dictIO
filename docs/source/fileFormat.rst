File Format
=================

dictIO reads and writes Python dicts as human-readable ASCII text files.
Default file format follows the C++ dictionary (cpp) notion. However dictIO partly reads and writes also OpenFOAM, Json and XML.

.. toctree::
   :maxdepth: 2

   fileFormat.cppDict
